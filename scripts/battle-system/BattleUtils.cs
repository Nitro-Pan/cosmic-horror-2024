namespace BattleUtils
{
    public enum StatusType
    {
        None,
        Dying,
        Moving,
        Passing
    }

    public enum HitType
    {
        None,
        Hit,
        Missed,
        Blocked
    }
}
