namespace BattleUtils
{
    public enum StatusType
    {
        None,
        Dying,
        Moving,
        Passing,
        Pushing,
        Pulling
    }

    public enum HitType
    {
        None,
        Hit,
        Missed,
        Blocked
    }
}
